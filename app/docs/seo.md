# SEO Technical Notes

## Editable Production Values

Replace these placeholder values before publishing the site:

- `https://clubpadel04.example/` in `index.html` canonical, Open Graph URL, image URL and JSON-LD `url`/`@id`.
- `CONFIGURAR_ZONA_REAL` in JSON-LD only when the real operating area is confirmed.

## Data Not Added Intentionally

The SEO metadata does not include address, phone or email because no verified real values are present in this repository.

Add those fields to JSON-LD only after the real club data is confirmed.

## Social Image

`public/og-image.svg` is an honest abstract fallback image. Replace it with a real branded social image when available.
